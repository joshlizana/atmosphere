---
id: 01KPZC7ESN4EKNX1XMNYTDSDPE
type: file
name: uv.lock
created_at: 2026-04-24T09:13Z
created_by: log/01KPZC7ESN4EKNX1XMNYTDSDPE
component: repo
---

## Purpose
Tracks the lifecycle of `/home/josh/atmosphere/uv.lock` — the deterministic, universal dependency lockfile generated by `uv lock` against the root `pyproject.toml`. It is the canonical pinned dependency set the whole workspace reproduces from, and is deliberately carved out of `.gitignore` so the committed lockfile is the single source of truth for what every CI run and every fresh clone resolves against.

## 2026-04-24T09:13Z — initial
- agent: log/01KPZC7ESN4EKNX1XMNYTDSDPE
- refs: [[files/JDL-65/pyproject-toml]], [[files/JDL-65/gitignore]], [[research/JDL-65/setup-uv-workspace-ci-2026]]

Forge created `uv.lock` at the repo root as part of JDL-65 M0 scaffolding by running `uv lock` against the newly-landed root `pyproject.toml`. The command reported `Resolved 69 packages in 1.05s` with exit code 0 and no resolution errors; `pyproject.toml` was not modified in the process. The resulting file is 90 KB, parses as valid TOML (verified via `tomllib.loads`), and carries top-level keys `version`, `revision`, `requires-python`, and `package`, with 69 `[[package]]` entries covering the transitive closure of the `dev` dependency group declared in the root manifest (`pytest`, `ruff`, `pre-commit`, `dbt-core`, `dbt-duckdb`). ^p001

The lockfile landed in commit `4ac521d` on branch `JDL-65-m0-repo-ci-host-prep` and was staged with an explicit `-- uv.lock` pathspec on the `git commit` invocation so the unrelated working-tree churn (deleted `vault/research/JDL-M0/` notes, the untracked root `CLAUDE.md`, the new `vault/research/JDL-65/setup-uv-workspace-ci-2026.md`, `.claude/`, and `vault/.obsidian/`) stayed out of the commit. Forge noted the index was clean at commit time and described the pathspec as belt-and-suspenders given the untracked and deleted files in the working tree. ^p002

The invocation was deliberately plain — `uv lock` with no flags, no `--upgrade`, and no environment-variable overrides — matching the Design-level constraint that the first lock of a fresh workspace captures a clean resolution rather than one steered by ad-hoc tuning. The host toolchain was `uv 0.11.7` with Python 3.12.13 selected to satisfy the workspace's `requires-python = "==3.12.*"` constraint declared in `pyproject.toml`. ^p003

This file is a deliberate carve-out of `.gitignore` — the `# uv` section in that file explicitly keeps `uv.lock` tracked while ignoring the rest of uv's local state, for the reason recorded in that file's initial entry: ignoring the lockfile would break the "one commit describes one reproducible dependency graph" property the platform relies on for CI reproducibility and contributor dev-loop parity. With this file landed alongside `pyproject.toml`, a fresh clone can now run `uv sync` and converge on the exact same 69-package environment Forge resolved here. ^p004
